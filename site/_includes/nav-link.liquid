<a href="{{ url }}"
  title="{{ title }}"
  class="
    block
    border-none
    font-emphasized
    hover:bg-{{ color }}-200
    hover:text-{{ color }}-700
    mb-1
    px-3
    py-2
    rounded-normal
    small:inline
    small:mb-0

    {% if page.url == url %}
      bg-{{ color }}-200
      text-{{ color }}-700
    {% else %}
      bg-transparent
      text-gray-600
    {% endif %}
  ">
  <span class="mr-2">{{- icon -}}</span>
  {{- title -}}
</a>
